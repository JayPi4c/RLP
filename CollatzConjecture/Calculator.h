#include "Arduino.h"
#include "Vars.h"





extern unsigned long calculate(unsigned long i);

extern int getDigits(int value);

extern String Time(unsigned long startMillis, unsigned long currentMillis);

extern String toString(int input);
